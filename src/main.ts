// ---------- entry point: start the standalone editor against the page's scaffold ----------

import { startEditor } from "./app.js";

// The standalone app reads its palette from the document root and loads the built-in default hull.
startEditor();
